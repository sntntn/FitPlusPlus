import axios from "axios";


export default {
    methods: {
        login(user, pw) {
                          
        },

        register(firstname, lastname, username, password, email, phonenumber) {
            
        }
    }
}
